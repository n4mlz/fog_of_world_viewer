services:
  viewer:
    container_name: fogofworld-viewer
    build:
      context: .
      args:
        - DOMAIN=${DOMAIN}
    volumes:
      - ./data:/data
    ports:
      - "${PORT}:${PORT}"
    command: ["python", "main.py", "/data"]
